# Anthropic API Key — used by the /api/gifts serverless function (never exposed to the browser)
# Get your key at: https://console.anthropic.com/
#
# For local development with `vercel dev`: add this to .env
# For production: add via Vercel Dashboard → Settings → Environment Variables
ANTHROPIC_API_KEY=sk-ant-your-api-key-here
